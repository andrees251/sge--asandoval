<article class="evento-card">
    <a href="{{ path('app_evento_detalle', {'slug': evento.slug}) }}">
       <h3>
        {{ evento.titulo|title }}
        </h3>
    </a>
    <p>{{ evento.descripcion|slice(0,200) }}</p>



</article>